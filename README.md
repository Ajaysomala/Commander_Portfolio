# Commander Portfolio

React + Vite portfolio for **Somala Ajay** — ready to deploy.

## Quick setup (local)

```bash
# install
npm install

# dev server
npm run dev
```

Open the local URL printed by Vite (usually http://localhost:5173).

## Build

```bash
npm run build
npm run preview
```

## Deploy

Recommended: **Cloudflare Pages** (link your GitHub repo and use build command `npm run build`, output `dist`).

Alternate: GitHub Pages (use your preferred workflow / gh-pages package).

## Notes

- Put your real resume at `public/Somala-Ajay-Resume.pdf` (already included as a placeholder).
- Update `src/CommanderPortfolio.jsx` SOCIAL links (Naukri / Instagram) with your real profile URLs.
